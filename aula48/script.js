const relogio = window.document.querySelector('section#relogio');
const visorCronometro = relogio.querySelector('time#visor');

relogio.onclick = (e) => {
    const el = e.target;
    
    if(el.classList.contains('btn-iniciar')) {
        this.alert('Botão iniciar!');
    }

    if()
}